enum COIN{
    HEAD = 1,
    TAIL = 'T'
}

console.log(COIN);
console.log(COIN.HEAD);
console.log(COIN[1]);

console.log("Type of Enum is : "+(typeof COIN));

/* TODO: By Students:
* Create and enum of weekdays and assign it a string value
* and display the entire enum, just one constant using key and value 
*/